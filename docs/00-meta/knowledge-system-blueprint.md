---
id: meta-knowledge-system-blueprint
note_type: blueprint
status: active
domain: meta
updated: 2026-06-29
---

# Knowledge System Blueprint

`docs/`는 이 프로젝트의 **단일 진실 원천(SSoT)** 이자, 동시에 배포 산출물인
**에이전트 실행용 기획문서(GDD + build-spec)** 다. 게임을 만드는 과정에서 훅으로 갱신되어
항상 최신 정본을 유지한다.

## 산출물 3종 (이 레포가 담는 것)

1. **기획문서(정본)** — `docs/` 의 번호 섹션들. 프로젝트 정본 본문.
2. **게임 프로젝트 파일** — `game/` (HTML5 프로토타입; 스택 결정 후 생성).
3. **파이프라인 인프라 + 지시 원본** — `docs/00-meta/` (훅 규칙, 템플릿, **input-log**).

## 토폴로지 (프로젝트 문서 구조에 정렬)

| 폴더                   | 문서 범위             | 내용                                   |
| -------------------- | -------------------- | ------------------------------------ |
| `00-meta/`           | — (운영)               | 파이프라인 규칙, 노트 스키마, 템플릿, **input-log** |
| `10-concept/`        | ① 컨셉 + 핵심 재미 가설      | 컨셉, 타겟, 검증가능한 재미 가설                  |
| `20-core-loop/`      | ② Core Loop + 플레이 흐름 | 코어 루프 정의, 세션 플로우, 온보딩                |
| `30-systems/`        | (③ 입력)               | 코어 루프를 구성하는 상세 시스템/메카닉               |
| `40-balancing/`      | ④-a 밸런싱 수치           | 명시적 수치, 곡선, 드랍/비용 테이블                |
| `50-art-ux/`         | ④-b 아트·UX 가이드        | 아트 방향, UX 판단 가이드                     |
| `60-implementation/` | ③ 구현 지시              | 기술스택·구조·단계별 태스크 분해·에이전트 런북           |
| `70-verification/`   | ⑤ 검증 기준              | KPI / 프로토타입 완성 체크리스트                 |
| `80-research/`       | (근거)                 | 레퍼런스 게임 분석, 시장 데이터                   |
| `game/`              | ③ 산출물                | 실제 HTML5 프로토타입 (스택 미정 → 예약)          |

## 파이프라인 (log → reconcile → reflect → work)

게임을 만드는 동안 문서가 자동으로 최신 정본을 유지하게 하는 핵심 장치. 읽기는 **`docs-find`**
스킬, 쓰기는 **`docs-write`** 스킬로 수행한다.

1. **log** — 모든 사용자 지시를 `00-meta/input-log/<날짜>.md` 에 **그대로** 기록
   (`UserPromptSubmit` 훅, 무조건).
2. **reconcile** — 지시가 기존 문서와 다르면 **해당 섹션 문서를 먼저 수정**한다.
3. **reflect** — 턴 끝에 이번 턴에서 *결정/추가*된 GDD 지식을 알맞은 섹션으로 정리한다.
   soft 규칙 `.claude/rules/docs-auto-reflect.md` 가 무엇을·어디에를 안내하고, `Stop` 훅
   (`docs-reconcile-check.mjs`)이 누락을 잡으며 **후보 섹션을 지목**한다.
4. **work** — 수정된 문서를 근거로 작업(설계/구현)한다.

전체 규칙: `.claude/rules/docs-pipeline.md`.

## 핵심 규칙

1. **input-log는 신성하다.** 손으로 편집 금지 — 지시(요청)의 원본.
2. **요청과 정본을 분리한다.** 요청은 `input-log/`, 결정/설계는 번호 섹션 문서.
3. 한 문서는 한 주제(컨셉/루프/시스템/결정)에 집중한다.
4. **새 지시가 문서와 충돌하면 문서를 먼저 고친 뒤 작업한다.**
5. **미정인 것을 단정하지 않는다.** 장르 세부·스택 등 미정 → `status: draft` + 열린 질문.
6. **에이전트 실행성**이 최우선 기준이다. 모든 섹션은 "이대로 에이전트에게 주면 실행되는가"로 검증.
