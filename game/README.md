# game/ — HTML5 prototype (reserved)

The playable HTML5 merge prototype goes here. **Empty by design** — the tech stack is
not decided yet.

- Stack decision (pixi.js / vanilla Canvas / three.js) is tracked in
  [`../docs/60-implementation/tech-stack.md`](../docs/60-implementation/tech-stack.md).
- The agent build plan that fills this folder is
  [`../docs/60-implementation/task-breakdown.md`](../docs/60-implementation/task-breakdown.md)
  and the runbook [`../docs/60-implementation/agent-runbook.md`](../docs/60-implementation/agent-runbook.md).

When the stack is chosen, scaffold here (e.g. `index.html`, `src/`, `assets/`, build
config) and keep it the single runnable artifact — `docs/` stays the design source of
truth, `game/` is what it produces.
